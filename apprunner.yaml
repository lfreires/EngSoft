version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "build ok"

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
  command: gunicorn --bind 0.0.0.0:8000 app:app
  network:
    port: 8000
